Frontend:
- Open frontend/index.html in your browser.
- It calls the Spring Boot API at http://localhost:8080 by default.
- Change API variable in frontend/js/room.js to point to the Spring Boot host if needed.
